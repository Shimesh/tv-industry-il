rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Simple catch-all: any authenticated user can read/write anything
    // TODO: tighten rules after confirming Firestore works
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
